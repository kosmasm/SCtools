- hosts: all
  remote_user: root
  gather_facts: yes
  any_errors_fatal: true
  tasks:
    - name: Set up multiple authorized keys
      ansible.posix.authorized_key:
        user: deploy
        state: present
        key: '{{ item }}'
      with_file:
        - authorized_keys
